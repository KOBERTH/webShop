export type UseIntersectionProps = {
  root?: Element;
  rootMargin?: string;
  threshold?: number;
  cb?: (arg0: unknown) => void;
}

export type productCardProps = {
  id: string;
  img: string;
  name: string,
  price: string;
}